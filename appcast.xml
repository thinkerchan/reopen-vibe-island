<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
    <channel>
        <title>ReOpenIsland Updates</title>
        <link>https://github.com/thinkerchan/open-vibe-island/releases</link>
        <description>Most recent changes with links to updates.</description>
        <language>en</language>
        <!--
            Items are populated by the release workflow once you push a `v*` tag
            to `main`. The fork ships without `SUPublicEDKey`, so Sparkle will
            refuse update payloads until you generate an EdDSA key pair, embed
            the public key in the bundle's Info.plist, and sign each release.
            Until then, the in-app "Check for updates" button is best-effort and
            users should download new versions manually from the Releases page.
        -->
    </channel>
</rss>
